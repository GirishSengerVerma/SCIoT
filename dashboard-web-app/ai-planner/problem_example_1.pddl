(define (problem weather-events-action-planning-problem-example-1)
    (:domain weather-events-action-planning)
    (:objects

    )

    (:init
        ;todo: put the initial state's facts and numeric values here
    )

    (:goal
        (and
            ;todo: put the goal condition here
        )
    )

    ;un-comment the following line if metric is needed
    ;(:metric minimize (???))
    ; TODO Goal: 
    ;           minimize sum of all current risks (most important) and 
    ;           maximize sum of operated locations (less important) and
    ;           maximize sum of current fuel levels and sum of equipment levels (least important)  (see e.g. https://planning.wiki/ref/pddl21)
)